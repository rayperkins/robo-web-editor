{
    "tasks": [
        {
            "type": "shell",
            "label": "Run Dev Server",
            "command": "npm run start -- --host 0.0.0.0 --port 4200",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],  
        },
        {
			"label": "Start devtunnel",
			"type": "shell",
			"command": "devtunnel user login;devtunnel host -p 4200 --allow-anonymous",
			"problemMatcher": []
		}
    ],
    "version": "2.0.0"
}